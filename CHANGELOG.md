# Changelog

## v0.2.0
### 重要修复
- **修复插件禁用/卸载后资源泄漏**：`shutdown()` 现在会正确清理所有菜单、事件监听器、Chrome 包注册和首选项面板。
- **修复删除功能兼容性**：`Zotero.Items.trashItems` API 在部分 Zotero 版本中不可用，改为三级降级方案（`Zotero.Items.trash` → `Zotero.Items.trashItems` → 逐条 `item.trash + saveTx`），确保删除功能在所有版本都正常工作。
- **修复非文献条目参与查重**：现在只对普通文献条目（`isRegularItem()`）进行查重，排除附件、笔记等非文献条目，避免误匹配。
- **修复规则语义混淆**：PDF 内容匹配改为可配置选项（默认开启以保持向后兼容），用户可在设置中关闭，选择「仅按 DOI」等规则时不会被 PDF 匹配干扰。
- **修复选项面板兼容性**：设置页从 XUL 格式改为纯 XHTML 格式，确保在 Zotero 7+ 中正常显示。

### 性能优化
- **并查集按秩合并**：新增 rank 数组，最坏情况从 O(n) 优化到接近 O(α(n))。
- **批量获取 PDF 附件**：使用 `Promise.all` 并行获取所有条目的 PDF 附件信息，大幅提升扫描速度。
- **对话框加载优化**：新增进度条提示，组内附件数量并行获取，UI 线程不再阻塞。

### 用户体验改进
- **关闭确认重新设计**：从两个模糊选项改为三个清晰选项（跳过所有未处理的组并关闭 / 继续处理 / 放弃所有更改并关闭）。
- **全部删除警告**：全局操作选择「全部删除」时显示醒目警告。
- **重复 PDF 卡片显示父条目标题**：便于用户理解上下文。
- **年份提取更准确**：使用正则匹配 4 位数字并验证在 1800-2100 范围内。
- **作者名使用方括号包裹**：避免姓名拼接产生的歧义匹配。
- **卸载时清理首选项**：卸载插件会自动清理相关首选项，不留残留。

### 构建系统
- **Windows 原生构建支持**：`build.mjs` 改为使用 Node.js 内置 zlib 模块构建 zip，不再依赖系统 `zip` 命令。

## v0.1.2
- 新增「按 PDF 内容（MD5）匹配」：即使两个 PDF 文件名不同、但字节内容相同，也能识别为重复（含同一篇条目下挂了多个相同 PDF 的情况）。
- PDF 内容匹配在所有查重规则下默认作为额外信号生效；设置中新增「仅按 PDF 内容」规则。
- 重复 PDF 组可在结果窗口中单独「保留一篇 / 删除多余 PDF / 跳过」，删除仅移入回收站。

## v0.1.1
- 修复查重检测过严导致无法识别重复文章的问题：改用「多候选键 + 并查集」的宽松匹配（相同标题即可归组）。
- 增加扫描诊断日志与「未发现重复」提示中的条目计数，便于排查范围/规则问题。

## v0.1.0
- 初始版本。
- 「工具」菜单「开始扫描（去重）」入口。
- 支持「当前收藏夹」与「当前选中条目所在分类」两种扫描范围。
- 支持 DOI / 标题+作者+年份 / 仅标题 三种查重规则。
- 单窗口结果展示，支持每组「保留一篇 / 全部删除 / 跳过」、批量应用、关闭确认。
- 删除仅移入回收站。
